// import { lazy } from "react";
import { Route, Routes } from 'react-router-dom';

// const SignInPage = lazy(() => import(''));

export const App = () => {
  return (
    <Routes>
      <Route path="" />
    </Routes>
  )
};

